package edivad.extrastorage.tools;

import dev.toliner.enhancedstorage.EnhancedStorage;

public class Translations
{
    public static String HOLD_SHIFT = "tooltip." + EnhancedStorage.MOD_ID + ".holdShift";
    public static String SLOT_CRAFTING = "tooltip." + EnhancedStorage.MOD_ID + ".slotCrafting";
    public static String BASE_SPEED = "tooltip." + EnhancedStorage.MOD_ID + ".baseSpeed";
}
